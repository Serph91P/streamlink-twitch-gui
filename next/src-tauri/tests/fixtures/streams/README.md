# Synthetic Streamlink JSON fixtures

These fixtures follow the top-level `--json` stream-map schema used by Streamlink 8.0 and 8.4. They are synthetic because public Twitch channels, codec rollouts and advertised qualities change nondeterministically. URLs and tokens are intentionally non-routable placeholders.
